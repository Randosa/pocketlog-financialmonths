version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    # PRs always target dev, never main (branching convention, see
    # CONTRIBUTING.md); main only updates via the dev -> main release PR.
    target-branch: "dev"
    # Bundle every github-actions update into a single monthly PR instead of
    # one PR per action. Avoids the mutual merge conflicts that separate PRs
    # cause once the first one lands (each touches the same pinned-SHA lines
    # in test.yml/build.yml/dev.yml).
    groups:
      github-actions:
        patterns:
          - "*"

  # Python dependencies (FastAPI, SQLAlchemy, Alembic, argon2-cffi …).
  - package-ecosystem: "pip"
    directory: "/backend"
    schedule:
      interval: "monthly"
    target-branch: "dev"
    groups:
      python:
        patterns:
          - "*"

  # Base image bumps (python:3.12-slim …).
  - package-ecosystem: "docker"
    directory: "/backend"
    schedule:
      interval: "monthly"
    target-branch: "dev"
    groups:
      docker:
        patterns:
          - "*"
